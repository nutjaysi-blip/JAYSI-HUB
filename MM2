-- ตรวจสอบและลบ UI เก่า (ถ้ามี)
local CoreGui = game:GetService("CoreGui")
if CoreGui:FindFirstChild("JaysiHubLoader") then
    CoreGui.JaysiHubLoader:Destroy()
end

-- สร้าง ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "JaysiHubLoader"
ScreenGui.Parent = CoreGui
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.IgnoreGuiInset = true -- สั่งให้หน้าต่างคลุมทับเต็มจอรวมถึงแถบด้านบนสุด

-- 1. หน้าต่างพื้นหลังเต็มจอ (ปรับให้ทึบขึ้นกว่าเดิม)
local FullScreenOverlay = Instance.new("Frame")
FullScreenOverlay.Name = "FullScreenOverlay"
FullScreenOverlay.Parent = ScreenGui
FullScreenOverlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
FullScreenOverlay.BackgroundTransparency = 0.35 -- ปรับให้ทึบขึ้น (ค่าน้อยลง = ทึบขึ้น)
FullScreenOverlay.Size = UDim2.new(1, 0, 1, 0)
FullScreenOverlay.Position = UDim2.new(0, 0, 0, 0)

-- หน้าต่างหลักตรงกลาง (ขนาดเล็ก สี่เหลี่ยม ขอบโค้ง)
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
MainFrame.Size = UDim2.new(0, 280, 0, 160)

-- ทำขอบโค้งให้หน้าต่างหลัก
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = MainFrame

-- เพิ่มขอบสีฟ้าให้กับหน้าต่างหลัก
local UIStroke = Instance.new("UIStroke")
UIStroke.Parent = MainFrame
UIStroke.Color = Color3.fromRGB(0, 170, 255) -- สีฟ้า
UIStroke.Thickness = 3 -- ความหนาขอบ

-- ใส่รูปพื้นหลังตาม ID ที่กำหนด
local BgImage = Instance.new("ImageLabel")
BgImage.Name = "BgImage"
BgImage.Parent = MainFrame
BgImage.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
BgImage.BackgroundTransparency = 1
BgImage.Size = UDim2.new(1, 0, 1, 0)
BgImage.Image = "rbxassetid://124215910892153"
BgImage.ScaleType = Enum.ScaleType.Stretch

local BgCorner = Instance.new("UICorner")
BgCorner.CornerRadius = UDim.new(0, 12)
BgCorner.Parent = BgImage

-- 2. เลเยอร์สีดำโปร่งใสทับบนรูปพื้นหลัง (ปรับให้ทึบขึ้นเพื่อให้ปุ่มและตัวหนังสือเด่นชัด)
local DarkOverlay = Instance.new("Frame")
DarkOverlay.Name = "DarkOverlay"
DarkOverlay.Parent = MainFrame
DarkOverlay.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
DarkOverlay.BackgroundTransparency = 0.3 -- ปรับให้ทึบขึ้น (ถ้าอยากให้ทึบกว่านี้ลดลงอีก เช่น 0.2)
DarkOverlay.Size = UDim2.new(1, 0, 1, 0)

local DarkCorner = Instance.new("UICorner")
DarkCorner.CornerRadius = UDim.new(0, 12)
DarkCorner.Parent = DarkOverlay

-- ข้อความหัวข้อ
local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Parent = DarkOverlay
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0, 0, 0, 10)
Title.Size = UDim2.new(1, 0, 0, 25)
Title.Font = Enum.Font.GothamBold
Title.Text = "JAYSI HUB"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16

-- ปุ่มปิดหน้าต่าง (กากบาท)
local CloseBtn = Instance.new("TextButton")
CloseBtn.Name = "CloseButton"
CloseBtn.Parent = DarkOverlay
CloseBtn.BackgroundColor3 = Color3.fromRGB(231, 76, 60)
CloseBtn.Position = UDim2.new(1, -30, 0, 10)
CloseBtn.Size = UDim2.new(0, 20, 0, 20)
CloseBtn.Font = Enum.Font.GothamBold
CloseBtn.Text = "X"
CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseBtn.TextSize = 12

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseBtn

-- ปุ่มกดเข้าดิสคอร์ด
local DiscordBtn = Instance.new("TextButton")
DiscordBtn.Name = "DiscordButton"
DiscordBtn.Parent = DarkOverlay
DiscordBtn.BackgroundColor3 = Color3.fromRGB(88, 101, 242)
DiscordBtn.Position = UDim2.new(0.1, 0, 0.4, 0)
DiscordBtn.Size = UDim2.new(0.8, 0, 0, 35)
DiscordBtn.Font = Enum.Font.GothamBold
DiscordBtn.Text = "Discord (Copy Link)"
DiscordBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
DiscordBtn.TextSize = 14

local DiscordCorner = Instance.new("UICorner")
DiscordCorner.CornerRadius = UDim.new(0, 8)
DiscordCorner.Parent = DiscordBtn

-- ปุ่มกดรันสคริปต์
local RunBtn = Instance.new("TextButton")
RunBtn.Name = "RunButton"
RunBtn.Parent = DarkOverlay
RunBtn.BackgroundColor3 = Color3.fromRGB(46, 204, 113)
RunBtn.Position = UDim2.new(0.1, 0, 0.68, 0)
RunBtn.Size = UDim2.new(0.8, 0, 0, 35)
RunBtn.Font = Enum.Font.GothamBold
RunBtn.Text = "Run Script"
RunBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
RunBtn.TextSize = 14

local RunCorner = Instance.new("UICorner")
RunCorner.CornerRadius = UDim.new(0, 8)
RunCorner.Parent = RunBtn

-- ฟังก์ชันการทำงานของปุ่มปิด
CloseBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- ฟังก์ชันการทำงานของปุ่มดิสคอร์ด
DiscordBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard("https://discord.gg/2FE96qXDdX")
        DiscordBtn.Text = "Copied Link!"
        task.wait(1.5)
        DiscordBtn.Text = "Discord (Copy Link)"
    end
end)

-- ฟังก์ชันการทำงานของปุ่มรันสคริปต์
RunBtn.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/nutjaysi-blip/JAYSI-HUB/refs/heads/main/MM2"))()
end)
